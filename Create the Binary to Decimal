#include <iostream>
#include <math.h>
using namespace std;
 
int main()
{
  long bi, dec = 0, i = 1, r;
	cout << " Input a binary number: ";
	cin>> bi;
  while (bi != 0) 
  {
   r = bi%10;
   dec = dec + r*j;
   i = i*2;
   bi = bi/10;
  }
  cout<<" The decimal number: " << dec<<"\n";
} 

