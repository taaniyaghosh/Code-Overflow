# impport library
import os
import glob

# get current working directory
my_folder = os.getcwd()
my_folder

# list directory
list_entries = os.listdir(my_folder)
list_entries

#making folders
for i in range(1, 11):
    new_folder = my_folder + r'\testfolder' + str(i)
    os.mkdir(new_folder)
    print('created' + new_folder)
    
# making list of folders
list_folders = []
for entry in new_dir:
    if(os.path.isdir(entry)):
        list_folders.append(entry)
list_folders

# searching for folders
list_testfolders = glob.glob("testfolder*")
list_testfolders

#deleting directory
for folder in list_testfolders:
    os.rmdir(folder)
os.listdir()
